diff --git a/src/lerobot/policies/perceptron_isaac/modeling_qwen35_vla.py b/src/lerobot/policies/perceptron_isaac/modeling_qwen35_vla.py
--- a/src/lerobot/policies/perceptron_isaac/modeling_qwen35_vla.py
+++ b/src/lerobot/policies/perceptron_isaac/modeling_qwen35_vla.py
@@ -1,5 +1,6 @@
 # ruff: noqa
+import os
 """Local HF model definition for the Isaac Qwen3.5-VL flow-matching VLA.
@@ -999,3 +1000,29 @@
+\n+# [PATCH] Isaac advanced train scopes. The native helper freezes the backbone
+# for expert-only mode; this post-hook selectively re-enables the state/vector
+# connector for connector fine-tuning without changing the upstream objective.
+_ISAAC_NATIVE_SETUP_QWEN35 = setup_qwen35_vla_for_training
+
+
+def setup_qwen35_vla_for_training(*args, **kwargs):
+    _ISAAC_NATIVE_SETUP_QWEN35(*args, **kwargs)
+    scope = os.environ.get("ISAAC_TRAIN_SCOPE", "expert")
+    if scope not in {"expert_connector", "expert_connector_lora"}:
+        return
+    model = args[0] if args else kwargs.get("model")
+    if model is None:
+        raise RuntimeError("ISAAC advanced training scope requires the model argument.")
+    found = 0
+    for name, parameter in model.named_parameters():
+        if "model.vector_embedding." in name:
+            parameter.requires_grad_(True)
+            found += parameter.numel()
+    if found == 0:
+        raise RuntimeError("ISAAC connector scope requested but model.vector_embedding parameters were not found.")
*** a/src/lerobot/policies/perceptron_isaac/modeling_perceptron_isaac.py b/src/lerobot/policies/perceptron_isaac/modeling_perceptron_isaac.py
--- a/src/lerobot/policies/perceptron_isaac/modeling_perceptron_isaac.py
+++ b/src/lerobot/policies/perceptron_isaac/modeling_perceptron_isaac.py
@@ -1,12 +1,13 @@
 """Perceptron Isaac VLA policy for LeRobot native eval/serving.
@@
 import builtins
 import hmac
 import json
 import logging
+import os
 import shutil
@@ -805,12 +806,15 @@
         if self.config.use_peft:
@@
             for name, parameter in self._isaac_model.named_parameters():
                 if "action_expert" in name:
                     parameter.requires_grad_("modules_to_save.default" in name)
                 elif "lora_" in name:
                     parameter.requires_grad_(True)
+                elif "vector_embedding." in name and os.environ.get("ISAAC_TRAIN_SCOPE") in {"expert_connector", "expert_connector_lora"}:
+                    parameter.requires_grad_(True)
         self._promote_trainable_parameters_to_fp32(self._isaac_model)
-        groups: dict[str, list[Tensor]] = {"vlm": [], "vision": [], "expert": []}
+        groups: dict[str, list[Tensor]] = {"vlm": [], "vision": [], "expert": [], "connector": []}
         for name, parameter in self._isaac_model.named_parameters():
@@
             if "action_expert" in name:
                 groups["expert"].append(parameter)
+            elif "vector_embedding." in name:
+                groups["connector"].append(parameter)
             elif name.startswith("model.visual."):
                 groups["vision"].append(parameter)
@@
         for name, lr in (
             ("vlm", self.config.optimizer_lr),
             ("vision", self.config.optimizer_vit_lr),
             ("expert", self.config.optimizer_action_expert_lr),
+            ("connector", float(os.environ.get("ISAAC_CONNECTOR_LR", self.config.optimizer_action_expert_lr))),
         ):
             if groups[name]:
                 result.append({"params": groups[name], "lr": lr})
@@
         for name, parameter in peft_model.named_parameters():
             if "action_expert" in name:
                 parameter.requires_grad_("modules_to_save.default" in name)
             elif "lora_" in name:
                 parameter.requires_grad_(True)
+            elif "vector_embedding." in name and os.environ.get("ISAAC_TRAIN_SCOPE") in {"expert_connector", "expert_connector_lora"}:
+                parameter.requires_grad_("modules_to_save.default" in name or "vector_embedding." in name)
*** End Patch
